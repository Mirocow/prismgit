/**
 * Core dictionary — app-wide shared strings (common actions, navigation,
 * sidebar, toolbar, settings chrome, toasts). Seeded before the domain sweep;
 * per-domain strings live in ./domains/<domain>.ts.
 */
import { en as enDict } from './en';
import { ru as ruDict } from './ru';
import { zh as zhDict } from './zh';
import { de as deDict } from './de';

export const en = enDict;
export const ru = ruDict;
export const zh = zhDict;
export const de = deDict;
